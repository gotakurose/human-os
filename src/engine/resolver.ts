// Phase 1 implementation: resolves normalized scores → typeId
// Reads scoring.json rules and applies dominant-axis matching.

import type { AxisScoreMap } from "./scorer";

export function resolveType(
  _scores: AxisScoreMap,
  _scoringRules: unknown
): string {
  // TODO: implement in Phase 1 engine work
  throw new Error("resolver.ts: not yet implemented");
}
